---
# This playbook Installs httpd service

- hosts: localhost
  remote_user: root
  become: yes
  become_method: sudo
  tasks:
  - name: Install the httpd rpm
    yum: name=httpd state=present
  - name: start the httpd service
    service: name=httpd state=started

